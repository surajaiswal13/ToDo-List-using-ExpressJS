<%- include("header") -%>

    <!-- <% if (listTitle === "Saturday" || listTitle === "Sunday") { %>
        <h1 style="color: purple">It's a <%= listTitle %>!</h1>
    <% } else { %>
        <h1 style="color: blue">It's a <%= listTitle %>!</h1>
    <% } %> -->

    <div class="box" id="heading">

      <h1> <%= listTitle %> </h1>

    </div>

    <div class="box">
      <% for(var i=0; i<newlistItem.length; i++) { %>

      <form action="/delete" method="post">

        <div class="item">
          <input type="checkbox" name="checkbox" value="<%= newlistItem[i]._id %>" onChange="this.form.submit()">
          <p> <%= newlistItem[i].item %> </p>
        </div>

      </form>

      <% } %>

      <form class="item" action="/" method="post">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off20wwwwwwo">
        <button type="submit" name="list" value="<%= listTitle %>">+</button>
      </form>

    </div>

<%- include("footer") -%>
